Odpri konzolo Anaconda prompt

$ cd do direktorija

Za nastavitev pozeni:

$ python preparation.py -c conf_prepare.ini

Za prikaz korelacijske funkcije in individualno meritev poženi:

$ python live_measurement.py -c conf_measure.ini